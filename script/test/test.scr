test(a, b)
{
	x = 1.5 + a;

	_ret_ = x;	# return
}

main()
{
	a = -2.+4.*2; 
	b = (75+25)^2;
	c = a+b + 0.2;
	d = (not(not((1 and 0) or 1)));

	if (d neq 1)
		str = "Teststring \t 1";
	else
	{
		str = "Teststring \t 2";
		str2 = "abcdef";
	}

	tst = test(a,2);
}
