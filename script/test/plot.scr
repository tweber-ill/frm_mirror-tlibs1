main()
{
	i=0;
	while(i less 500)
	{
		strTitle = "Parameter: " + i;

		ph = i/500.;
		x = linspace(0, 2*pi, 64);
		y = sin(x + ph);

		plot(x,y, ["xlabel" : "x", "ylabel" : "y", "ylimits" : "-1.1 1.1", "title" : strTitle]);

		i = i+1;
	}
}
