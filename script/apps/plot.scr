#!./hermelin
# Plotting

mcplot2d(file)
{
	fulldat = loadtxt(file);
	numcols = length(fulldat);

	params = map();
	if(typeof(fulldat[-1]) == "map")
	{
		params = fulldat[-1];
		numcols -= 1;
	}

	dat = fulldat[0:numcols];

	dat = trans(dat);
	numlines = length(dat)/3;
	dat = dat[0:numlines];

	plot2d(dat, params);
}

mcplot(file)
{
	dat = loadtxt(file);
	plot(dat[0],dat[1],dat[2],dat[-1]);
}


main(args)
{
	mcplot2d(args[1]);
}
